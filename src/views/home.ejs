<!DOCTYPE html>
    <head>
        <meta charset="UTF-8">
        <title>Home</title>
    </head>
    <body>
        <h1>Home</h1>
        <% for(var i=0; i<users.length; i++) {%>
            <li><%= users[i].email %></li>
        <% } %>
        <a href="/logout">Se déconnecter</a>
    </body>
</html>